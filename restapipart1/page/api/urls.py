
from django.contrib import admin
from django.urls import path
from django.conf import urls
from page.api import views
# from.views import statuslistsearchapi
urlpatterns = [
#    path('/',statuslistsearchapi.as_view()),

    # path('create/',statuscreate.as_view()),
    # url(r'^(?p<id>.*)/update/$',statusupdate.as_view()),
    # url(r'^(?p<id>.*)/delete/$',statusdelete.as_view()),

]
